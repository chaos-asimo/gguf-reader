# GGUF 推理 GUI 验收标准

## 一、构建与依赖

- [ ] **C1**: `cargo build --release --features gui` 编译成功，无 error
- [ ] **C2**: 生成 `target/release/gguf-gui.exe`（Windows）
- [ ] **C3**: `cargo build --release`（不带 gui feature）仍编译成功，不引入 eframe/egui 依赖
- [ ] **C4**: `cargo test --release` 全部 192 个现有测试通过（库 API 兼容性）
- [ ] **C5**: `cargo test --release --features gui` 通过（含新增 GUI 相关测试）
- [ ] **C6**: `gguf-infer` 和 `gguf-dump` 两个 bin 仍可正常构建运行

## 二、模型加载

- [ ] **M1**: 工具栏 📂 按钮打开文件对话框，过滤 `*.gguf` 文件
- [ ] **M2**: 选择有效 GGUF 文件后，模型在后台线程加载，UI 不卡顿
- [ ] **M3**: 加载成功后状态指示变为 🟢 就绪，显示模型名称
- [ ] **M4**: 加载成功后自动填充模型信息标签页（摘要、KV、张量）
- [ ] **M5**: 选择无效文件（不存在/非 GGUF 格式）显示 🔴 错误状态 + 错误信息
- [ ] **M6**: 加载大模型（>500MB）时显示"加载中..."状态
- [ ] **M7**: 切换模型时旧模型正确释放内存
- [ ] **M8**: 推理线程 panic 时 UI 显示"推理线程异常"而非崩溃

## 三、对话功能

- [ ] **CH1**: 模型加载后可在对话标签页发送消息
- [ ] **CH2**: 发送后流式 token 实时追加到助手气泡
- [ ] **CH3**: 多轮对话上下文通过 KV cache 自动累积
- [ ] **CH4**: 对话格式使用 Qwen2 chat template（im_start/im_end/user/assistant）
- [ ] **CH5**: 中文输入输出正常（UTF-8）
- [ ] **CH6**: [重置上下文] 按钮清空 KV cache + 对话历史，UI 对话区清空
- [ ] **CH7**: [停止生成] 按钮可中断当前推理，已生成内容保留
- [ ] **CH8**: 生成中输入框禁用，停止/完成后恢复
- [ ] **CH9**: 上下文超出限制时显示 ⚠️ 提示 + [重置] 按钮
- [ ] **CH10**: 对话历史切换标签页后不丢失
- [ ] **CH11**: 空输入不发送
- [ ] **CH12**: 连续 5 轮对话无重复、无退化（repeat penalty 基于完整上下文）

## 四、Prompt 补全功能

- [ ] **P1**: Prompt 输入框支持多行文本
- [ ] **P2**: [运行] 后调用 `Engine::generate()` 生成补全
- [ ] **P3**: 流式输出时 token 实时追加到输出区
- [ ] **P4**: 取消勾选"流式输出"后一次性返回完整文本
- [ ] **P5**: 勾选"贪心解码"后覆盖温度/top-k/top-p
- [ ] **P6**: 显示耗时、token 速率、ctx 用量
- [ ] **P7**: [清除输出] 按钮清空输出区
- [ ] **P8**: [停止生成] 按钮可中断
- [ ] **P9**: 每次运行从 position 0 全量 prefill（无状态）

## 五、模型信息功能

- [ ] **V1**: 文件摘要显示：文件名、架构、模型名、GGUF 版本、对齐、数据偏移、文件大小、张量数、KV 数
- [ ] **V2**: KV 元数据表格显示键/类型/值
- [ ] **V3**: KV 搜索框可过滤指定键（对应 `--key`）
- [ ] **V4**: 张量列表显示名称/形状/类型
- [ ] **V5**: 张量列表默认前 50，☐ 显示全部可展开（对应 `--tensors-all`）
- [ ] **V6**: 词表查询支持按 Token ID（对应 `--token-id`）
- [ ] **V7**: 词表查询支持按 Token 字符串（对应 `--token-str`）
- [ ] **V8**: [JSON] 导出按钮生成 JSON 文件（对应 `--json --pretty`）
- [ ] **V9**: [复制摘要] 按钮将摘要文本复制到剪贴板

## 六、设置面板

- [ ] **S1**: 温度滑块 0.0–2.0，默认 0.8
- [ ] **S2**: Top-K 输入 0–1000，默认 40
- [ ] **S3**: Top-P 滑块 0.0–1.0，默认 0.95
- [ ] **S4**: Min-P 滑块 0.0–1.0，默认 0.0
- [ ] **S5**: 重复惩罚滑块 1.0–2.0，默认 1.1
- [ ] **S6**: 种子输入 0–u64，默认 0
- [ ] **S7**: 最大 Token 输入 1–32768，默认 512
- [ ] **S8**: 贪心 Checkbox，默认 false
- [ ] **S9**: 贪心勾选时温度/top-k/top-p/min-p 控件禁用
- [ ] **S10**: 参数修改后立即生效于后续生成（无需重载模型）
- [ ] **S11**: seed 变更时重置 RNG

## 七、工具栏与状态栏

- [ ] **T1**: 📂 选择模型按钮
- [ ] **T2**: 模型名称下拉框显示当前模型
- [ ] **T3**: [加载] 按钮
- [ ] **T4**: [卸载] 按钮释放模型
- [ ] **T5**: 状态指示 🟢 就绪 / 🟡 生成中 / 🔴 错误
- [ ] **ST1**: 状态栏显示 ctx 用量/上限
- [ ] **ST2**: 状态栏显示 token 速率
- [ ] **ST3**: 状态栏显示最近一轮耗时
- [ ] **ST4**: 状态栏显示模型名

## 八、库 API 扩展

- [ ] **A1**: `Engine::generate_cancellable` 方法存在，`on_token` 返回 `false` 时中止
- [ ] **A2**: `Engine::chat_cancellable` 方法存在，`on_token` 返回 `false` 时中止
- [ ] **A3**: `Sampler::set_config` 方法存在，seed 变更时重置 RNG
- [ ] **A4**: 现有 `Engine::generate` / `Engine::chat` 行为不变（回归测试通过）
- [ ] **A5**: 新增方法有单元测试

## 九、跨平台

- [ ] **X1**: Windows 编译运行正常
- [ ] **X2**: `windows-sys` 依赖通过 `cfg(windows)` 隔离，Linux/macOS 不受影响
- [ ] **X3**: `rfd` 文件对话框在 Windows 下正常弹出
